---
- hosts: all

  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
    - name: check if system reboot ist required
      become: true
      stat:
        path: /var/run/reboot-required
      register: reboot_required
